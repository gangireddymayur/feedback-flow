# ===== ReviewOS Backend =====
PORT=3001
NODE_ENV=production

# MySQL (create the DB in Plesk → Databases first)
DB_HOST=localhost
DB_PORT=3306
DB_USER=reviewos_user
DB_PASSWORD=changeme
DB_NAME=reviewos

# Auth
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=7d

# CORS — your Lovable frontend domain(s), comma separated
CORS_ORIGINS=https://your-frontend.lovable.app,http://localhost:5173
