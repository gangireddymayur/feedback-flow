{
  "name": "reviewos-server",
  "version": "1.0.0",
  "description": "ReviewOS — Express + MySQL backend (Plesk Node.js compatible)",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node scripts/migrate.js",
    "build:frontend": "echo 'Frontend is deployed on Vercel; Plesk only runs the API.'",
    "postinstall": "echo 'Dependencies installed. Run migrate once, then restart the Plesk Node.js app.'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.0",
    "zod": "^3.23.8"
  }
}
